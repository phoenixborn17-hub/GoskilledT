// Skeleton for /courses/[slug] — matches the re-skinned detail layout (content + sticky card).
import { DetailSkeleton } from "../../../components/marketing/page-skeleton";

export default function CourseDetailLoading() {
  return <DetailSkeleton />;
}
