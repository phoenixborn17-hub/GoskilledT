// Skeleton for /courses — matches the re-skinned layout (shell + cover-band cards), zero CLS.
import { CardsGridSkeleton } from "../../components/marketing/page-skeleton";

export default function CoursesLoading() {
  return <CardsGridSkeleton />;
}
